apiVersion: apps/v1
kind: Deployment
metadata:
  name: estoque-lv
spec:
  replicas: 1
  selector:
    matchLabels:
      app: estoque-lv
  template:
    metadata:
      labels:
        app: estoque-lv
    spec:
      containers:
        - name: estoque-lv
          image: 955465104750.dkr.ecr.us-east-1.amazonaws.com/estoque-lv
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: mws-config

            - secretRef:
                name: mws-db-secret
---
apiVersion: v1
kind: Service
metadata:
  name: estoque-lv
spec:
  selector:
    app: estoque-lv
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80